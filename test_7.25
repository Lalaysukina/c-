int main()
{
	int a;
	char pd[20] = {};
	for (a = 1; a <= 3; a++)
	{
		scanf("%s", &pd);
		if (strcmp(pd ,"123456") == 0)   //strcmp是比较字符串是否相同的函数
		{
			printf("登陆成功\n");
			break;
		}
		else
			printf("密码错误,请重新输入\n");
		if (a == 3)
			printf("输入错误次数过多，请稍后再试\n");
	}
	return 0;
}
